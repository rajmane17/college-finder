const { asyncHandler } = require("../utils/asyncHandler");

const handleAddCollege = asyncHandler((req, res) => {
    //get the details about the college from the user
    //
})

module.exports = {handleAddCollege}